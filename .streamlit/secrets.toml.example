# Copy these keys into Streamlit Community Cloud's Secrets editor.
# Do not commit a real .streamlit/secrets.toml file.

# Authenticated account API used by the Streamlit frontend.
CONSENSUS_SCOPE_BACKEND_URL = "https://api.consensusscope.cn"

# Provider credentials belong only in the separately hosted FastAPI backend
# environment. Never put them in Streamlit Secrets or browser-facing code.
